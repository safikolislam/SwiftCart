1) What is the difference between null and undefined?
Ans: null is a value assigned by the user intentionally to represent no value and undefined is automatically assigned by javascript.

2) What is the use of the map() function in JavaScript? How is it different from forEach()?
Ans: forEach  and map are used instead of loop to iterate over arrays and map return a new array and forEach return does not return anything.


3) What is the difference between == and ===?
Ans: == means it checks only the value after type conversion and === means it checks both value and type without type conversion.

4) What is the significance of async/await in fetching API data?
Ans: async/await is used to handle asyncronous operation like fetching API data.It makes asyncronous code like syncronous code and helps to handle promise easily and easiyer error handaling;

5) Explain the concept of Scope in JavaScript (Global, Function, Block).

Ans: Global scope is declared in the global scope and can be acessed anywhere in the document  .Function Scope: Variables declared inside a function can be accessed only within that function and block scope: variables declared inside a block like curly brackets can be accessed only inside the block;